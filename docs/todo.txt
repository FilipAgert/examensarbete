CODE TODO:
Parallelisera att räkna ut sannolikheter
PROBLEM: 

 "Array 'evals' at (1) is larger than limit set by '-fmax-stack-var-size=', moved from stack to static storage. 
 This makes the procedure unsafe when called recursively, or concurrently from multiple threads. 
 Consider increasing the '-fmax-stack-var-size=' limit (or use '-frecursive', which implies unlimited '-fmax-stack-var-size') - 
 or change the code to use an ALLOCATABLE array. If the variable is never accessed concurrently, this warning can be ignored, 
 and the variable could also be declared with the SAVE attribute. [-Wsurprising]",

Lincomb does not work.




TODO:
Testa mot random walk
Lägg till spin term

börja skriva rapport:
Bakgrund. 
Förklara koordinaterna I J K L M
Jämför metoder
Random walk vs iterativ multiplikation vs arpack

Jämför linear interpolation

